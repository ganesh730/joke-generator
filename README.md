Joke Generator Project

1)Add an event listener for the button
2)Create a function that will make a request to https://api.chucknorris.io/jokes/random using the XMLHttpRequest object
3)Get the data that is sent back (this.responseText), parse the JSON and get the joke from it
4)Display the joke in the page (you can use the innerHTML property)
